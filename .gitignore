# config.js is deliberately NOT ignored. GitHub Pages serves only what is
# committed, so the app needs it in the repo to work at all. The Firebase config
# is a project identifier rather than a password — see README. The security
# rules are what protect the data.
.firebase/
.firebaserc
node_modules/
.DS_Store
Thumbs.db
.claude/settings.local.json
